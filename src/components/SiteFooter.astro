---
import { socialLinks } from "../config/links";
---

<footer class="mt-24 border-t border-white/10 py-12 px-4">
  <div class="max-w-7xl mx-auto flex flex-col gap-8 text-center sm:flex-row sm:items-center sm:justify-between sm:text-left">
    
    <p class="text-[12px] uppercase tracking-[0.25em] text-gray-500 font-medium">
      &copy; {new Date().getFullYear()} <span class="text-fuchsia-500/80 font-bold">Mr. Bonzo</span>. 
      <span class="block sm:inline sm:ml-2 opacity-60" data-i18n="footer.rights">All rights reserved.</span>
    </p>

    <div class="flex flex-wrap items-center justify-center gap-x-8 gap-y-4 sm:justify-end">
      {[
        { name: "Instagram", href: socialLinks.instagram },
        { name: "YouTube", href: socialLinks.youtube },
        { name: "Email", href: socialLinks.email },
        { name: "TikTok", href: socialLinks.tiktok },
        { name: "Spotify", href: socialLinks.spotify }
      ].map((link) => (
        <a
          href={link.href}
          target={link.name !== "Email" ? "_blank" : undefined}
          rel={link.name !== "Email" ? "noopener noreferrer" : undefined}
          class="text-[12px] font-black uppercase tracking-[0.2em] text-gray-400 transition-all duration-300 hover:text-fuchsia-400 hover:translate-y-[-2px]"
        >
          {link.name}
        </a>
      ))}
    </div>
    
  </div>
</footer>